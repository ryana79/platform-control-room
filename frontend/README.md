# AzurePlatform Frontend

Next.js App Router portal for self-service workload onboarding, policy validation, GitOps visibility, Kubernetes status, cost governance, and drift detection.

```bash
cd frontend
npm install
npm run dev
```
