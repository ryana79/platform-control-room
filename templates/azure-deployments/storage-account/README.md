# Storage Account Template

Generates a secure Azure Storage account with public blob access disabled, TLS 1.2 minimum, and mandatory platform tags.

The generated GitLab pipeline runs Terraform format, validation, and plan. Apply is manual/protected.
