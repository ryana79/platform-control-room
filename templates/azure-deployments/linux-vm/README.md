# Linux VM Template

Generates a small Azure Linux VM plan with resource group, virtual network, subnet, network interface, and Ubuntu VM resources.

The generated pipeline is intended for plan review. Apply remains manual/protected to avoid accidental cost.
