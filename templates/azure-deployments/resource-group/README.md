# Resource Group Template

Generates a tagged Azure resource group for a team-owned workload landing zone.

The generated GitLab pipeline runs Terraform format, validation, and plan. Apply is manual/protected.
