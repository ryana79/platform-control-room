# Workload Onboarding

A developer submits a workload request. The backend stores it in SQLite, generates Terraform tfvars, Helm values, ArgoCD Application YAML, Kubernetes manifests, policy results, and a cost estimate.
